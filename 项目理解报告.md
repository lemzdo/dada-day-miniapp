# 搭一搭（d1d）项目理解报告

> 生成时间：2026-05-15  
> 原则：只分析、不修改代码

---

## 一、项目概览

**项目名称**: 搭一搭（d1d）

**产品定位**: AI 驱动的穿衣搭配助手——用户拍照上传衣服到云端衣柜，系统根据天气、场景、风格偏好，结合 AI 视觉识别与大语言模型，自动生成每日穿搭推荐。

**终端形态**:
- **主战场**：微信小程序（Taro 4.x + React 18）
- **辅助端**：Next.js 15 Web 应用（React 19）—— 兼顾 BFF 层 + 管理后台

**当前阶段**: Phase 0 已完成（架构搭建 + 类型定义 + Mock API + 空壳页面），核心业务功能尚未实现。

---

## 二、架构总览

```
d1d/
├── apps/
│   ├── web/          # Next.js 15 App Router (React 19)
│   └── miniapp/      # Taro 4.x 微信小程序 (React 18)
├── packages/
│   ├── types/        # 共享 TypeScript 类型定义
│   ├── utils/        # SSR-safe 工具函数
│   ├── api/          # 共享 API 客户端 + 端点函数
│   ├── ai/           # AI 能力抽象层（多 Provider）
│   ├── hooks/        # 通用 React Hooks
│   ├── auth/         # 认证上下文 Provider
│   └── ui/           # 共享 UI 组件库（TailwindCSS）
├── database/
│   └── migrations/   # PostgreSQL 数据库迁移脚本
├── pnpm-workspace.yaml
├── turbo.json
└── tsconfig.base.json
```

---

## 三、技术栈明细

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **包管理** | pnpm | 9.15.0 | workspace 协议管理 |
| **构建编排** | TurboRepo | 2.4.4 | 并行构建 + 缓存 |
| **语言** | TypeScript | 5.6.3 | strict 模式，全链路类型安全 |
| **Web 框架** | Next.js | 15.1.0 | App Router，BFF + 前端 |
| **Web React** | React | 19.0.0 | Web 端渲染 |
| **小程序框架** | Taro | 4.0.9 | 微信小程序编译（目标 weapp + H5） |
| **小程序 React** | React | 18.3.1 | 小程序端渲染（Taro 不支持 React 19） |
| **状态管理** | Zustand | 5.0.0 | 小程序端用户状态 |
| **样式方案** | TailwindCSS | 3.4.17 (web) + SCSS (小程序) | 双端各自样式体系 |
| **CSS 预处理** | SCSS | - | 小程序端（设计稿 750px → rpx） |
| **ESLint** | 9.x flat config | - | 全局代码规范 |
| **Prettier** | 3.3.3 | - | 格式化（singleQuote, trailingComma:all, printWidth:100） |
| **数据库** | PostgreSQL | - | 含 pgcrypto 扩展 |
| **Node 要求** | Node.js | >= 20 | - |

### AI 供应商（规划中）

| 供应商 | 模型 | 能力 | API Key 环境变量 |
|--------|------|------|-----------------|
| SiliconFlow (硅基流动) | Qwen2.5-VL-72B | 视觉识别（衣服图片→结构化信息） | `SILICONFLOW_API_KEY` |
| DeepSeek | deepseek-chat | 穿搭推荐 + 衣柜分析（文本/JSON） | `DEEPSEEK_API_KEY` |
| DashScope (阿里百炼) | - | 备选识别 + 文案生成 | `DASHSCOPE_API_KEY` |

### 外部服务（规划中）

| 服务 | 用途 | 环境变量 |
|------|------|----------|
| 和风天气 | 实时天气 + 预报 | `QWEATHER_API_KEY` |
| 微信开放平台 | 小程序登录 code2Session | `WECHAT_APPID` / `WECHAT_SECRET` |
| 阿里云 OSS | 图片存储 | `OSS_*` 系列 |

---

## 四、已有功能（已完成模块）

### 4.1 基础设施 ✅

**类型层 (`packages/types`)** — 零运行时依赖，纯类型定义：
- 通用类型：`User`, `ApiResponse<T>`, `PaginationParams`, `PaginatedResult<T>`
- 认证类型：`AuthState`, `LoginParams`, `LoginResult`
- UI Prop 类型：`ButtonVariant`, `ButtonSize`, `ModalSize`, `ToastOptions`, `Toast`
- 请求状态类型：`RequestState<T>`, `MutationState<T>`
- **穿搭领域类型**（核心）：`Clothing`, `Outfit`, `OutfitHistory`, `WardrobeAnalysis`, `RecommendRequest/Response`, `ShareInput/Result`
- 枚举/联合类型：`ClothingCategory`, `ClothingSubcategory`, `Season`, `StyleTag`, `SceneTag`, `Material` 等
- 字典类型：`CategoryNode`, `SceneDictItem`, `StyleDictItem`
- 天气类型：`CurrentWeather`, `DailyForecast`, `WeatherSnapshot`
- AI 交互类型：`AiRecognitionResult`, `RecommendOutput`, `AnalyzeOutput`, `ScoreExplanation`

**工具层 (`packages/utils`)**:
- `cn()` — Tailwind class 合并
- `debounce()` / `throttle()` / `clamp()` — 通用函数
- `formatDate()` / `formatNumber()` — 格式化
- `storage.get/set/remove` — localStorage 封装（SSR 安全，自动 JSON 序列化）
- `isServer` / `isBrowser` / `isWeapp` — 环境检测
- `env` — 类型安全环境变量读取
- `normalizeError()` — 错误标准化

**API 客户端 (`packages/api`)**:
- `ApiClient` 类：基于 fetch 的 HTTP 客户端，支持 GET/POST/PUT/DELETE
- 拦截器链：请求/响应拦截器，自动注入 Authorization header
- 加载状态管理：自动 loading 计数 + Toast 通知
- **关键亮点**：双平台兼容 —— 浏览器用 `fetch()`，小程序动态 import `@tarojs/taro` 的 `Taro.request`
- `safeRequest()` — 不抛异常模式，返回 `{ data, error, ok }`
- 已封装的端点函数：`getUsers/CRUD`, `wechatLogin`, `getUserProfile/update`, `getUserCapacity`, `getCurrentWeather`, `getWeatherForecast`, `getCategories`, `getScenes`, `getStyles`

**React Hooks (`packages/hooks`)**:
- `useDebounce` / `useThrottle` — 防抖节流
- `useLocalStorage` — localStorage 持久化状态
- `usePrevious` / `useIsMounted` — 生命周期辅助
- `useMediaQuery` — 响应式监听
- `useAsync` / `useLoading` — 异步状态管理

**UI 组件库 (`packages/ui`)** — TailwindCSS v3 + React 18/19 双兼容:
- `Button`（5 variants × 3 sizes）
- `Card`（4 padding 档位）
- `Input` / `Textarea`（含 error/disabled 状态）
- `Modal`（5 尺寸 + closeOnBackdrop 配置）
- `Spinner` / `PageLoading` / `Skeleton` / `SkeletonGroup`（加载态）
- `Navbar` / `NavbarBack`（导航栏）
- `Tabbar`（底部标签栏）
- `ToastProvider` / `useToast`（全局提示）
- `ErrorBoundary`（错误边界）
- `Empty`（空状态）
- Design Tokens：`colors`, `spacing`, `fontSize`, `breakpoints`, `radius`, `shadow`, `zIndex`

**认证层 (`packages/auth`)**:
- `AuthProvider` — React Context 认证状态管理
- `useAuth()` — 消费 Hook
- 特性：token/user 持久化到 localStorage，自动注入 Authorization 头到 API 请求

### 4.2 Web 应用 (`apps/web`) ✅

- **BFF 层**: Next.js App Router API Routes（Mock 实现）
  - `POST /api/v1/auth/wechat-login` — 微信登录（Phase 0 返回 mock token）
  - `GET/PUT /api/v1/user/profile` — 用户信息（mock）
  - `GET /api/v1/user/capacity` — 容量查询（mock）
  - `GET /api/v1/weather/current` — 实时天气（mock）
  - `GET /api/v1/weather/forecast` — 天气预报（mock）
  - `GET /api/v1/dict/categories` — 衣服分类树（静态硬编码）
  - `GET /api/v1/dict/scenes` — 场景列表（静态硬编码）
  - `GET /api/v1/dict/styles` — 风格标签（静态硬编码）
- **前端页面**: 仅 `/` 首页，展示登录/登出 Demo（AuthProvider + UI 组件集成验证）
- **全局布局**: `RootLayout` 包裹 `ToastProvider` + `AuthProvider`

### 4.3 小程序应用 (`apps/miniapp`) ✅

**Tab 页（4 个，实际展示 3 个）**:

| Tab | 路由 | 名称 | 状态 |
|-----|------|------|------|
| 1 | `pages/today/index` | 今日 | UI 骨架 + WeatherCard 组件、穿搭推荐区为空 |
| 2 | `pages/wardrobe/index` | 衣橱 | 容量条 + 分类横滑 + 空状态 |
| 3 | `pages/profile/index` | 我的 | 用户卡片 + 数据统计 + 功能入口菜单 + VIP 入口 |
| 4 | `pages/index/index` | 首页 | 废弃壳子（Hello World 文本） |

**已实现组件**:
- `WeatherCard` — 天气卡片（调用 `/api/v1/weather/current`，含 Skeleton 加载态 + 降级数据）

**状态管理**:
- `useUserStore`（Zustand）— 用户登录（含容错 mock 登录）、logout、fetchProfile、风格偏好设置

**小程序入口**: `app.tsx` 启动时自动调用 `login()`；`app.config.ts` 注册 4 个页面 + 底部 TabBar 配置（3 个 tab）

### 4.4 数据库 Schema ✅

**一次建完 7 张表**（`001_initial_schema.sql`），通过 `pgcrypto` 生成 UUID 主键：

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `users` | 用户表 | 微信 openid/unionid、风格档案 JSONB、容量/会员 |
| `clothes` | 衣服表 | 图片 URL、AI 识别结果 JSONB、用户自定义标签、容量消耗、使用统计 |
| `outfits` | 穿搭方案表 | 衣服 ID 数组、场景/日期/时段、天气快照、AI 评分、是否收藏/今日穿着 |
| `outfit_history` | 穿搭历史表 | 穿着日期、满意度评分、备注、唯一约束(user,date,tod) |
| `share_records` | 分享记录表 | 分享类型（friend/moments/image） |
| `reminders` | 提醒设置表 | 推送时间、星期、雨天/降温提醒开关 |
| `wardrobe_analyses` | 衣柜分析报告表 | 风格/颜色/品类分布、Top 使用/闲置物品、缺失建议 |
| `weather_cache` | 天气缓存表 | 唯一约束(location_key, date)、过期时间 |

**索引**: 在 `user_id`、`category`、`status`、`target_date`、`wear_date` 等常用查询条件上建立了 BTREE 索引。

---

## 五、未完成模块（Phase 1-3 待实现）

### 5.1 AI 服务层 — 🔴 全部为桩代码

| 文件 | Provider | 已实现能力 | 桩状态 |
|------|----------|-----------|--------|
| `packages/ai/src/providers/siliconflow.ts` | SiliconFlow | **无** | 4 个方法全部 `throw Error(...not implemented)` |
| `packages/ai/src/providers/deepseek.ts` | DeepSeek | **无** | 4 个方法全部 `throw Error(...not implemented)` |

需要实现的 AIService 接口方法：
1. **recognizeClothing** — 调用 Qwen-VL 识别衣服图片，返回品类/颜色/风格/季节/材质
2. **recommendOutfit** — 规则引擎粗排 + DeepSeek 精排，生成穿搭方案（含评分+解释）
3. **analyzeWardrobe** — 衣柜统计分析 + LLM 总结，产出缺失建议
4. **generateCopywrite** — 为分享生成社交文案（目标 Provider: DashScope）

**缺少**: 没有 `dashscope` provider 文件、没有 AIService 的路由/编排层（路由 Policy）。

### 5.2 后端 API — 🟡 全量 Mock，未接入真实服务

| API 端点 | 当前状态 | 待实现 |
|----------|----------|--------|
| `auth/wechat-login` | Mock 固定 token | 接入微信 code2Session，创建/匹配用户 |
| `user/profile` | Mock 静态数据 | 读写 PostgreSQL `users` 表 |
| `user/capacity` | Mock 固定值 | 实时计算 `capacity_used` |
| `weather/current` | Mock 天气 | 接入和风天气 API + `weather_cache` 表 |
| `weather/forecast` | Mock 预报 | 同上 |
| `dict/*` | 硬编码数组 | 当前已足够，后续可数据库化 |

**完全缺失的 API 端点**（类型已定义，但路由未创建）：
- ❌ `POST /api/v1/clothes` — 上传衣服 + 触发 AI 识别
- ❌ `GET/PUT/DELETE /api/v1/clothes/:id` — 衣服 CRUD
- ❌ `GET /api/v1/clothes` — 衣服列表（分页、筛选、排序）
- ❌ `POST /api/v1/outfits/recommend` — 生成穿搭推荐
- ❌ `GET /api/v1/outfits` — 穿搭列表
- ❌ `PUT /api/v1/outfits/:id` — 更新穿搭（收藏、标记今日穿着等）
- ❌ `POST /api/v1/outfits/:id/wear` — 记录穿搭历史
- ❌ `GET /api/v1/outfits/history` — 穿搭历史列表
- ❌ `POST /api/v1/outfits/:id/share` — 生成分享图/文案
- ❌ `GET /api/v1/wardrobe/analysis` — 触发/获取衣柜分析
- ❌ `PUT /api/v1/reminders` — 提醒设置 CRUD

### 5.3 小程序功能 — 🟡 全部空壳

| 页面 | 已实现 | 待实现（Phase 1/2/3） |
|------|--------|----------------------|
| **今日** | WeatherCard + 空状态 | 穿搭推荐卡片展示、场景选择器、换一换、标记穿着、分享入口 |
| **衣橱** | 容量条 + 分类横滑 + 空状态 | 衣服上传（拍照/相册）、衣服列表网格、分类筛选、衣服详情、删除/编辑、AI 识别结果展示 |
| **我的** | 用户卡片 + 统计 + 菜单壳 | 风格偏好设置页、穿搭历史页、收藏列表、衣柜分析报告页、提醒设置页、会员订阅 |

**缺少的页面**（未创建路由）:
- ❌ 衣服详情页
- ❌ 添加/编辑衣服页
- ❌ 穿搭详情页
- ❌ 穿搭历史列表页
- ❌ 衣柜分析报告页
- ❌ 风格偏好选择页
- ❌ 分享生成页

### 5.4 数据库访问层 — 🔴 完全缺失

- ❌ 没有 ORM 或 query builder（如 Drizzle、Prisma、Knex）
- ❌ 没有数据库连接池管理
- ❌ 没有任何 CRUD Repository 或 Service 层
- 当前所有 API 直接返回硬编码 JSON，未连接数据库

### 5.5 图片处理与存储 — 🔴 完全缺失

- ❌ 图片上传流程（小程序选择图片 → 上传到 OSS → 生成缩略图）
- ❌ OSS 客户端封装
- ❌ 图片预处理（压缩、裁剪）

### 5.6 DevOps / CI — 🟡 空白

- ❌ 没有 CI/CD 配置（如 GitHub Actions）
- ❌ 没有 Docker 配置
- ❌ 没有环境变量管理策略（仅 `.env.example`）
- ❌ 没有自动化测试（单元/集成/E2E）
- ❌ 没有监控/日志方案

---

## 六、依赖关系图

```
types (零依赖)
  ↑
  ├── utils (零运行时依赖)
  │     ↑
  │     └── api ──── 依赖 types + utils
  │           ↑
  │           ├── auth ──── 依赖 types + utils + api
  │           │     ↑
  │           │     └── apps/web ──── 依赖全部 packages
  │           │
  │           └── apps/miniapp ──── 依赖 types + utils + api (不含 auth/ui/hooks)
  │
  ├── hooks ──── 零依赖，纯 React hooks
  │
  ├── ui ──── 仅 peerDeps react，双端通用组件
  │
  └── ai ──── 依赖 types，独立于 apps
```

**关键约束**:
- 小程序依赖 React 18，`packages/ui` 必须兼容 React 18
- 小程序不能直接用 `fetch()`，`packages/api` 通过 `isWeapp` 检测并切换到 `Taro.request`
- `packages/types` 禁止引入运行时依赖
- 跨包引用必须使用 `workspace:*` 协议 + `@starter-template/*` 包名

---

## 七、Phase 规划分析（从代码注释推断）

| Phase | 内容 | 状态 |
|-------|------|------|
| **Phase 0** | 项目初始化、架构搭建、类型定义、Mock API、空壳页面、数据库建表 | ✅ 完成 |
| **Phase 1** | 衣服上传 + AI 视觉识别（SiliconFlow Qwen-VL）+ 衣橱 CRUD | 🔴 未开始 |
| **Phase 2** | 穿搭推荐引擎（规则粗排 + DeepSeek 精排）+ 今日页面完整交互 | 🔴 未开始 |
| **Phase 3** | 衣柜分析（统计分析 + LLM 总结）+ 穿搭历史 + 分享 + 提醒 | 🔴 未开始 |

---

## 八、代码质量评估

| 维度 | 评分 | 说明 |
|------|------|------|
| **类型安全性** | ⭐⭐⭐⭐⭐ | strict 模式 + noUncheckedIndexedAccess，全链路类型定义 |
| **架构一致性** | ⭐⭐⭐⭐⭐ | monorepo 分层清晰，每层职责明确，AGENTS.md 规范详尽 |
| **可维护性** | ⭐⭐⭐⭐ | barrel export 模式统一，命名规范一致 |
| **API 设计** | ⭐⭐⭐⭐ | ApiResponse 统一包装，双平台兼容设计 |
| **UI 组件完备度** | ⭐⭐⭐⭐ | 常用组件齐全，多状态支持（loading/empty/error） |
| **后端完整性** | ⭐⭐ | 全量 Mock，零数据库连接，零认证校验 |
| **小程序完备度** | ⭐⭐ | UI 骨架完整，但全部是静态空壳 |
| **AI 层** | ⭐ | 仅接口定义 + 桩代码，零实际调用 |
| **测试覆盖** | ⭐ | 无任何测试用例 |
| **运维配套** | ⭐ | 无 CI/CD、Docker、监控 |

---

## 九、开发优先级建议

> 以下仅为分析建议，实际开发计划需另行确定。

1. **Phase 1 核心闭环**：数据库连接 → 衣服上传 API → OSS 图片存储 → AI 识别实现 → 衣橱列表/详情/删除
2. **Phase 2 核心闭环**：天气真实接入 → DeepSeek 推荐实现 → 今日页面完整交互 → 穿搭历史记录
3. **Phase 3 体验增强**：衣柜分析 → 分享功能 → 提醒推送 → 风格偏好 → 会员体系
4. **质量保障**：单元测试 + 集成测试 + CI/CD

---

## 十、项目文件统计

| 类别 | 文件数 | 位置 |
|------|--------|------|
| 类型定义 | 5 (.ts) | `packages/types/src/` |
| API 客户端/端点 | 5 (.ts) | `packages/api/src/` |
| AI Provider | 3 (.ts) | `packages/ai/src/` |
| 工具函数 | 6 (.ts) | `packages/utils/src/` |
| React Hooks | 9 (.ts) | `packages/hooks/src/` |
| Auth 组件 | 2 (.ts/.tsx) | `packages/auth/src/` |
| UI 组件 | 10 (.tsx) | `packages/ui/src/` |
| BFF API Route | 8 (.ts) | `apps/web/src/app/api/v1/` |
| 小程序页面 | 4 (组) | `apps/miniapp/src/pages/` |
| 小程序组件 | 1 (组) | `apps/miniapp/src/components/` |
| 数据库迁移 | 1 (.sql) | `database/migrations/` |
| 配置文件 | 15+ | 根目录及各子包 |
