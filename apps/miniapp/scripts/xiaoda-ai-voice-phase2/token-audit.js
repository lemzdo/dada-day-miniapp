'use strict';
function measure(value) { const s = typeof value === 'string' ? value : JSON.stringify(value); return { bytes: Buffer.byteLength(s, 'utf8'), chars: [...s].length, estimatedTokens: Math.ceil(Buffer.byteLength(s, 'utf8') / 4) }; }
function audit({ systemPersona = '', rules = '', forbiddenLanguage = '', goodExamples = '', badExamples = '', schema = '', repeatedInstructions = '', briefs = [] } = {}) { const sections = { systemPersona, rules, forbiddenLanguage, goodExamples, badExamples, schema, repeatedInstructions, stylingBriefPayload: briefs, perItemRepeatedContent: briefs.map(b => ({ scene: b.scene, inferenceBoundary: b.inferenceBoundary })), batchSharedContent: { promptShared: true, schemaShared: true, outputContractShared: true } }; return { estimated: true, sections: Object.fromEntries(Object.entries(sections).map(([k, v]) => [k, measure(v)])), total: measure(sections) }; }
function comparePayloads(v1Briefs, v2Briefs) { const baselineBytes = measure(v1Briefs).bytes; const v2Bytes = measure(v2Briefs).bytes; return { baselineBytes, v2Bytes, reductionBytes: baselineBytes - v2Bytes, reductionRatio: baselineBytes ? (baselineBytes - v2Bytes) / baselineBytes : 0 }; }
module.exports = { audit, comparePayloads, measure };
