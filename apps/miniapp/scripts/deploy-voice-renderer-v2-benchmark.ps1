throw @'
This legacy DevTools/inc-deploy benchmark path is retired because it can replace
production cloud artifacts without the canonical remote verification gate.
Formal deployment must use: pnpm cloud:deploy <generateOutfit|recommendationStream>
'@
