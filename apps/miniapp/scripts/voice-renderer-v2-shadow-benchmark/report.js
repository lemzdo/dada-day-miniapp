'use strict';
function summarize(artifact) { const rows = artifact.records || []; return { version: 'voice-renderer-v2-shadow-report-v1', status: artifact.status, scenes: artifact.scenes, repetitions: artifact.repetitions, recordCount: rows.length, byScene: Object.fromEntries((artifact.scenes || []).map((scene) => [scene, rows.filter((row) => row.scene === scene).map(compact)])), singleVsBatch: rows.reduce((out, row) => { out[row.mode] = (out[row.mode] || 0) + 1; return out; }, {}) }; }
function compact(row) { return { mode: row.mode, repetition: row.repetition, status: row.status, planCount: row.planCount, renderedCount: row.renderedCount, requestCount: row.requestCount, latencyMs: row.latencyMs, providerLatencyMs: row.providerLatencyMs, usage: row.usage, cacheHitCount: row.cacheHitCount, cacheMissCount: row.cacheMissCount, automatedContract: row.automatedContract, samePlanSet: row.samePlanSet, qualityNotDegraded: row.qualityNotDegraded }; }
module.exports = { summarize };
